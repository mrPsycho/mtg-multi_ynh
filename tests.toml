#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]
    args.port = 3128
    args.dns = "1.1.1.1"
    args.frontend_domain = "storage.googleapis.com"
    args.prometheus = false

    exclude = ["install.multi"]
